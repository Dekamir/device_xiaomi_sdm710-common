type shal_lineage_trust_default_server, coredomain, domain;
hal_server_domain(shal_lineage_trust_default_server, hal_lineage_trust_server)

type shal_lineage_trust_default_exec, exec_type, file_type;
init_daemon_domain(shal_lineage_trust_default_server)

# Talk to the binder device node
allow shal_lineage_trust_default_server binder_device:chr_file rw_file_perms;
